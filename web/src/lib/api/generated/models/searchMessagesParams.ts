/**
 * Generated by Orval. Do not edit manually.
 */

export type SearchMessagesParams = {
  /**
   * Search query
   */
  q: string;
  /**
   * Search mode: fts, vector, or hybrid. conversation_id applies in every mode; other structured filter parameters require vector or hybrid
   */
  mode?: string;
  /**
   * One-based page number (default 1; values below 1 are clamped to 1). Non-numeric values are rejected with 400.
   */
  page?: number;
  /**
   * Page size (default 20, max 100; out-of-range values are clamped). Non-numeric values are rejected with 400.
   */
  page_size?: number;
  /**
   * Zero-based ranking offset for vector or hybrid search (default 0)
   */
  offset?: number;
  /**
   * Include score explanation when mode is vector or hybrid
   */
  explain?: boolean;
  /**
   * Include scored semantic chunk excerpts for vector or hybrid results
   */
  include_matches?: boolean;
  /**
   * Minimum chunk score for included excerpts; does not filter ranked messages
   */
  min_score?: number;
  /**
   * Message type filter; repeat or comma-separate for multiple values
   */
  message_type?: string;
  /**
   * Restrict to one account/source
   */
  account?: string;
  /**
   * Restrict to one collection
   */
  collection?: string;
  /**
   * Exact conversation ID (all search modes)
   */
  conversation_id?: number;
  /**
   * Exact sender email/address filter (vector or hybrid mode only)
   */
  sender?: string;
  /**
   * Exact recipient email filter across to, cc, and bcc (vector or hybrid mode only)
   */
  recipient?: string;
  /**
   * Exact sender domain filter (vector or hybrid mode only)
   */
  domain?: string;
  /**
   * Exact case-insensitive label filter (vector or hybrid mode only)
   */
  label?: string;
  /**
   * Exact case-insensitive RFC 2919 List-Id filter (vector or hybrid mode only)
   */
  list_id?: string;
  /**
   * Calendar period in YYYY, YYYY-MM, or YYYY-MM-DD format (vector or hybrid mode only)
   */
  time_period?: string;
  /**
   * Time bucket granularity (vector or hybrid mode only)
   */
  time_granularity?: string;
  /**
   * Exact source ID (vector or hybrid mode only)
   */
  source_id?: number;
  /**
   * Only include messages with attachments (vector or hybrid mode only)
   */
  attachments_only?: boolean;
  /**
   * Lower date/time bound (RFC3339 or YYYY-MM-DD; vector or hybrid mode only)
   */
  after?: string;
  /**
   * Upper date/time bound (RFC3339 or YYYY-MM-DD; vector or hybrid mode only)
   */
  before?: string;
};
